// 2579. Count  Total Number of Colored Cells 

class Solution {
    public long coloredCells(int n) {
       if (n <= 1)
        return 1;
    
        double prev = Math.pow(n-1, 2);
        double current = Math.pow(n, 2);
        return (long) (prev + current);
    }
}